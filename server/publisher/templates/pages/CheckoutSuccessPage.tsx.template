'use client';

import React from 'react';
import Link from 'next/link';
import { useSearchParams } from 'next/navigation';
import theme from '@/theme.json';

export default function CheckoutSuccessPage() {
  const searchParams = useSearchParams();
  const orderId = searchParams.get('orderId');

  return (
    <div style={{ minHeight: '100vh', backgroundColor: theme.backgroundColor, fontFamily: theme.fontFamily, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <div style={{ textAlign: 'center', maxWidth: '500px', padding: '40px', backgroundColor: '#fff', borderRadius: '16px', boxShadow: '0 4px 24px rgba(0,0,0,0.1)' }}>
        <div style={{ width: '80px', height: '80px', backgroundColor: '#10b981', borderRadius: '50%', display: 'flex', alignItems: 'center', justifyContent: 'center', margin: '0 auto 24px' }}>
          <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="white" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round">
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
        </div>
        <h1 style={{ fontSize: '28px', fontWeight: 700, marginBottom: '16px' }}>Order Placed Successfully!</h1>
        <p style={{ fontSize: '16px', opacity: 0.7, marginBottom: '8px' }}>Thank you for your order.</p>
        {orderId && <p style={{ fontSize: '14px', opacity: 0.5, marginBottom: '32px' }}>Order ID: {orderId}</p>}
        <p style={{ fontSize: '14px', opacity: 0.7, marginBottom: '32px' }}>We will contact you shortly to confirm your order and arrange payment/delivery.</p>
        <Link href="/shop" style={{ display: 'inline-block', padding: '16px 32px', backgroundColor: theme.primaryColor, color: '#fff', borderRadius: '8px', fontWeight: 600, textDecoration: 'none' }}>
          Continue Shopping
        </Link>
      </div>
    </div>
  );
}
